#include <ctype.h>
#include <stdio.h>

/* loop executing getint p 96 */

#define SIZE 10

int main()
{

  int n, array[SIZE], getint(int *);
  
  for (n = 0; n < SIZE && getint(&array[n]) != EOF; n++)
    printf("%d ", array[n]);
  printf("\n");

}
